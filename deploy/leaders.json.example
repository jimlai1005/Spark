{
  "_comment": [
    "★★ 這是範例檔，部署時必須替換成真實內容 ★★",
    "位置：/opt/filet/spark/var/filet/leaders.json（見 RUNBOOK §5.5）",
    "權限：root:root 644 —— 必須是 filet-api 寫不到的位置。這是整道防線的承重點：",
    "  filet-api 若被打穿，攻擊者能改 pending.json 乃至 manifest，把 follower 指向",
    "  惡意 leader（對敲榨 builder fee／反向交易洗掉客戶資金）；他寫不到這份白名單，",
    "  所以引擎每輪拿白名單再驗一次才是真正擋下來的那道。白名單若可寫 = 防線歸零。",
    "",
    "兩個旗標是兩件事（搞混的代價：該止血的沒止／不該平倉的平了）：",
    "  enabled=false        安全撤銷 —— 這個 leader 現在有問題（帳號被盜、對敲、",
    "                       策略失控）。新客戶選不到，**且正在跟的 follower 立刻進入",
    "                       受控收尾**：撤單、reduce-only 全平、鎖死 kill switch。",
    "                       有實際 taker 成本、需人工 re-arm。只在真的要止血時用。",
    "  accepting_new=false  例行下架 —— 不再開放新客戶選擇（名額滿、策略調整中、",
    "                       準備退場）。新客戶選不到，**正在跟的人完全不受影響**。",
    "",
    "⚠️ 把條目整筆刪掉 **不等於** enabled=false（2026-07-27 起）：該位址若也在客戶自訂",
    "   准入的 user_leaders.json 裡，刪掉精選條目只會讓 registry 那筆 enabled=true 遞補",
    "   上來，撤銷靜默失效。只有「不在 registry 的位址」刪除才等同撤銷。",
    "   ⭐ 恆定有效的撤銷 = 確保精選檔存在一筆 enabled:false 的條目。請跑",
    "   `uv run python -m scripts.revoke_leader <address>`（冪等、跨兩檔、自帶驗收）。",
    "   例行下架請改 accepting_new，不要刪條目。",
    "",
    "kind（可省略）：leader 帳戶形態。合法值只有 'standard' 與 'vault'；缺欄位＝",
    "  standard（既有條目不用動）。'vault' 會讓 watcher 對新 follower 自動注入",
    "  20x 槓桿帽與流量中性化、引擎每輪按 kind 自衛。⚠️ vault 上架前必先跑",
    "  `uv run python -m scripts.vault_preflight <addr>` 六項全 PASS（RUNBOOK §5.5",
    "  「vault leader 上架前置檢查」）；拼錯值 → loader fail-fast 拒載整檔（保護，非 bug）。",
    "",
    "策略平台展示欄位（全部可省略，2026-08-28 改版）：slug（URL id，缺省時前端用完整",
    "  位址當 id）／tagline（策略卡一行文案）／tagline_en（英文版，2026-08-30 新增；",
    "  缺省時 EN 模式前端 fallback 用 tagline，不翻譯不留白）／featured（首頁主推",
    "  badge，bool）／min_notional_usd／max_leverage（展示用字串，不影響引擎實際",
    "  下單邏輯）。這六個是**白名單內可選欄位**——不在這份清單、也不在",
    "  address/name/description/enabled/accepting_new/kind 之內的鍵一律拒載整檔",
    "  （防拼字錯誤，見 spark/filet/leaders.py 檔頭「展示欄位」段）。",
    "",
    "格式為 fail-fast：JSON 壞掉／欄位型別錯／位址重複／未知欄位 → 引擎與 activate",
    "CLI 都會停，不會降級成空清單放行。改完務必用 `python -m json.tool` 驗一次再存。",
    "本 _comment 欄位會被 loader 忽略（只讀 leaders 陣列），但正式檔建議刪掉保持乾淨。"
  ],
  "leaders": [
    {
      "address": "0x1111111111111111111111111111111111111111",
      "name": "Alpha Trend",
      "description": "範例：中頻趨勢跟隨。正常營運中，開放新客戶。",
      "enabled": true,
      "accepting_new": true,
      "slug": "alpha-trend",
      "tagline": "多資產動能 · 永續合約",
      "tagline_en": "Multi-asset momentum · Perpetuals",
      "featured": true,
      "min_notional_usd": "500",
      "max_leverage": "3"
    },
    {
      "address": "0x2222222222222222222222222222222222222222",
      "name": "Beta Carry",
      "description": "範例：例行下架 —— 名額已滿，不收新客戶；既有 follower 照常跟單。",
      "enabled": true,
      "accepting_new": false
    },
    {
      "address": "0x3333333333333333333333333333333333333333",
      "name": "Gamma Legacy",
      "description": "範例：安全撤銷 —— 條目保留供歷史查核，跟隨者已被收尾。",
      "enabled": false,
      "accepting_new": false
    },
    {
      "address": "0xdeaddeaddeaddeaddeaddeaddeaddeaddeaddead",
      "name": "Delta Vault",
      "description": "範例：vault leader —— kind 標 vault（20x 帽＋流量中性化自動生效）。示範用 enabled:false：真實上架前必先跑 vault_preflight 六項全 PASS 再翻 true。",
      "enabled": false,
      "accepting_new": false,
      "kind": "vault"
    }
  ]
}
